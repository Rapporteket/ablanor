# This is a new file.
#
# A new change
# A third change, try again with git commands to commit and pusj

# 1) clone Abalnor
# 2) New project  - from existing directory (in ablanor directory)

# one more change


# Did theese commands (terminal) in home-directory, to see if
#      error message " load pubkey:::" disappears
# $ eval "$(ssh-agent -a)"
#  ssh-add ~/.ssh/id_rsa
# RESULT:         Identity added: /home/rstudio/.ssh/id_rsa (skkris@docker)
